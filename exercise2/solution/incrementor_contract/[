contract;

abi Incrementor {
    fn get(x: u64) -> u64;
    fn get2(b: u64) -> u64;
}

impl Incrementor for Contract {
    fn get(x: u64) -> u64 {
        x 
    }
    fn get2(b: b256) -> u64 {
        b 
    }
}
