contract;

use std::storage::{get, store, hash::{hash_u64, HashMethod}};
use incrementor_abi::Incrementor;

impl Incrementor for Contract {
    fn initialize(kinitial_value: u64) -> u64 {
       0 
    }

    fn increment(increment_by: u64) -> u64 {
        0 
    }

    fn get(key: u64) -> u64 {
        key
    }
}
